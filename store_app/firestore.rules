rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Completely open for debugging registration
    // TODO: Restore proper security rules after registration works
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
